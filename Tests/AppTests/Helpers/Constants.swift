import Foundation

struct Constants {
    struct Token {
        static let admin: String = "admin"
        static let consumer: String = "consumer"

        static let unknown: String = "this is not a valid token"
    }
}
